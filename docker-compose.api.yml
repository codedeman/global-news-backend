version: "3.9"

services:
  api:
    build: .
    ports:
      - "8080:8080"
    environment:
      DATABASE_URL: root:02111997@tcp(db:3306)/ai-agent-postgres?parseTime=true&charset=utf8mb4
    networks:
      - neura-net

networks:
  neura-net:
    external: true
